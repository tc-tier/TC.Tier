<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- 消费 Core.Net 预览 Raft wire API——随生产者 opt-in，压掉 CA2252 刷屏 -->
    <EnablePreviewFeatures>true</EnablePreviewFeatures>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\TC.Tier.Core.Net\TC.Tier.Core.Net.csproj" />
    <ProjectReference Include="..\..\src\TC.Tier.Products.Net\TC.Tier.Products.Net.csproj" />
  </ItemGroup>
  <ItemGroup>
    <!-- ★ FsRaftStore 单一真源链接（对抗套件同款——禁止复制） -->
    <Compile Include="..\..\tests\TC.Tier.Core.Net.Tests\Fixtures\FsRaftStore.cs" Link="Shared\FsRaftStore.cs" />
  </ItemGroup>
</Project>
