    /// <summary>编码（tag 判别 + 根前缀 + 子类字段）。返回新数组。</summary>
    public static byte[] Encode({{ROOT_NAME}} message)
    {
        var buffer = new byte[ComputeLength(message)];
        EncodeInto(buffer, message);
        return buffer;
    }
