    /// <summary>总长（单趟遍历——变长成员计数；池化/直写形态的长度预算口）。</summary>
    public static int ComputeLength({{ROOT_NAME}} message)
    {
        int cursor = 1;   // tag
        switch (message)
        {
{{LENGTH_CASES}}
            default: throw new System.ArgumentException($"未知消息类型：{message.GetType().Name}。", nameof(message));
        }
    }
