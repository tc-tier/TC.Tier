    /// <summary>编码（便捷形态——缓冲自配；返回新数组）。</summary>
    public static byte[] Encode(in {{TYPE_NAME}} value)
    {
        var buf = new byte[{{TOTAL_EXPR}} + 4 + value.{{ARRAY_FIELD}}.Length * {{ITEM_SIZE}}];
        Encode(buf, in value);
        return buf;
    }
