    /// <summary>解码（截断/Count 超上限（&gt;{{MAX_COUNT}}）= false——畸形报文拦截；逐字段防御）。</summary>
    public static bool TryDecode(System.ReadOnlySpan<byte> source, out {{TYPE_NAME}} value)
    {
        value = default;
{{BODY}}
        value = new {{TYPE_NAME}}({{CTOR_ARGS}});
        return true;
    }
