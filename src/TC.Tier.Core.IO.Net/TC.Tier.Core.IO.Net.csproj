<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>12.0</LangVersion>
    <RootNamespace>TC.Tier.Core.IO.Net</RootNamespace>
    <IsPackable>true</IsPackable>
    <PackageId>TC.Tier.Core.IO.Net</PackageId>
    <Description>TC.Tier Core/IO 网络传送层——根空间镜像的 TCP 流式收发（TCA1 载荷 + 握手/回执控制帧；四态互转的网络发射端）</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <!-- ★ 零外部依赖铁律：仅引用 TC.Tier.Core（与 IO.S3 同构——隔离网络副作用保 Core 纯计算） -->
  </PropertyGroup>
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>


  <ItemGroup>
    <ProjectReference Include="..\TC.Tier.Core\TC.Tier.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="TC.Tier.Core.IO.Net.Tests" />
  </ItemGroup>

</Project>
